apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "betterme-app.fullname" . }}-test-connection"
  labels:
    {{- include "betterme-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "betterme-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
